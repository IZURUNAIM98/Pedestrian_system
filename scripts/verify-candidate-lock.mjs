import { createHash } from "node:crypto"; import fs from "node:fs"; import path from "node:path"; const root=process.cwd(), manifest=JSON.parse(fs.readFileSync(path.join(root,"control/smartcross-candidate.lock.json"),"utf8")), failures=[]; for(const [relative,expected] of Object.entries(manifest.files)){const p=path.resolve(root,relative), rel=path.relative(root,p);if(rel.startsWith("..")||path.isAbsolute(rel)||!fs.existsSync(p)){failures.push(relative+": missing/outside");continue}if(createHash("sha256").update(fs.readFileSync(p)).digest("hex")!==expected)failures.push(relative+": hash mismatch")} if(failures.length){console.error(failures.join("\n"));process.exit(1)}console.log("SmartCross candidate lock verified ("+Object.keys(manifest.files).length+" files).");
